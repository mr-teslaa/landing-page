on:
    push:
        branches:
            - deploy
name: 🚀 Deploy website on push
jobs:
    web-deploy:
        name: 🎉 Deploy
        runs-on: ubuntu-latest
        steps:
            - name: 🚚 Get latest code
              uses: actions/checkout@v2

            - name: 📂 Sync files
              uses: SamKirkland/FTP-Deploy-Action@4.3.0
              with:
                  server: ftp.hossainfoysal.com
                  username: ${{ secrets.ftp_username_deploy }}
                  password: ${{ secrets.ftp_password_deploy }}